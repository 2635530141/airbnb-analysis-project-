# Airbnb Analysis Project

MaCSS

## Enviornment

```bash
python -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.txt
```

启动 Jupyter：

```bash
jupyter notebook
```

## Structure

- `data/raw/`：】
- `data/processed/`：
- `notebooks/`：
- `src/`：
- `outputs/`：

testing
